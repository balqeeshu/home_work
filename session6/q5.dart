void main() {
  //Create a program with a boolean isMember = false and an integer points = 120.
  bool isMember = false;
  int points = 120;

  // If isMember is true and points >= 100, print 'Eligible for reward'. Otherwise, print 'Not eligible'.
  if (isMember == true && points >= 100) {
    print('Eligible for reward');
  } else {
    print('Not eligible');
  }
}
